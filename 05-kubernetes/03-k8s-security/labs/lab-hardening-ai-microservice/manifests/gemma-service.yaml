apiVersion: v1
kind: Service
metadata:
  name: gemma-service
  namespace: gemma-secops
  labels:
    app: gemma-chat
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: gemma-chat
